description: Example flag used in Next.JS SSG
tags:
  - all

bucketBy:
  - userId

environments:

  build:
    rules:
      - key: "1"
        segments: "*" # enabled for everyone 
        percentage: 100

  live:
    rules:
      - key: "1"
        segments: "*" # enabled for everyone 
        percentage: 100


