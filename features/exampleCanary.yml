description: Example feature flag for canary releases in Production
tags:
  - all

bucketBy:
  - userId

environments:

  build:
    rules:
      - key: "1"
        segments: "*" # enabled for everyone 
        percentage: 100

  live:
    rules:
      - key: "1"
        segments: "*" # enabled for everyone 
        percentage: 100



