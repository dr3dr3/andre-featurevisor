description: Example feature flag for dark launching incomplete ode
tags:
  - all

bucketBy:
  - userId

environments:

  build:
    rules:
      - key: "1"
        segments: "*" # enabled for everyone 
        percentage: 100

  build-local:
    rules:
      - key: "1"
        segments: "*" # enabled for everyone 
        percentage: 100

