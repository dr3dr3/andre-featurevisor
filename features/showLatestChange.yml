description: Show latest change from last commit message
tags:
  - all

bucketBy:
  - userId

environments:

  build:
    rules:
      - key: "1"
        segments: "*" # enabled for everyone 
        percentage: 100

  live:
    rules:
      - key: "1"
        segments: "*" # enabled for everyone 
        percentage: 100
